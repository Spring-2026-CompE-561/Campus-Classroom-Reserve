![Campus Classroom Reserve Logo](frontend/public/SDSU_LOGO.png)
# Campus Classroom Reserve

Website for San Diego State University that students can use to find classrooms that are empty at the time, and book study sessions.

## Quickstart

Install both the back end and the front end (see installation instructions below). Then start both the backend and the frontend instances. View the backend documentation at [http://localhost:8000/docs](http://localhost:8000/docs). View the front end of the website at [http://localhost:3000](http://localhost:3000)!

### Starting the environment using Docker Compose

From the root directory (`/`), run the following:

```bash
docker compose up
```

## Installation

### Frontend

Located convienently in the `frontend` folder. See the [Frontend README](frontend/README.md) for installation and usage.

### Backend

Located convienently in the `backend` folder. See the [Backend README](backend/README.md) for installation and usage.
